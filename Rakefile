require "bundler/gem_tasks"
require "rake/extensiontask"


Rake::ExtensionTask.new "narray-types" do |ext|
  ext.lib_dir = "lib/narray-types"
end

Rake::ExtensionTask.new "narray" do |ext|
  ext.lib_dir = "lib/narray"
end
